archivesBaseName = 'purefun-optics'

dependencies {
  implementation project(':core')
  testImplementation libs.junitJupiter
  testImplementation libs.junitRunner
  testImplementation libs.equalsVerifier
}
